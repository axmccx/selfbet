import 'package:flutter/material.dart';

class GroupsBody extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: Text('Groups Body!'),
    );
  }
}
